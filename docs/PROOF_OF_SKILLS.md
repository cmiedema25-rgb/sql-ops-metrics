# Proof of Skills — SQL

- `queries.py` retains the exact SQL text for delayed-by-customer, lane delay
  rate, and status mix.
- `evidence/sql-ops-report.json` stores SQL + result rows a reviewer can re-run
  via `make report`.
